apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cromwell-local.fullname" . }}-test-connection"
  labels:
    {{- include "cromwell-local.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cromwell-local.fullname" . }}:{{ .Values.service.cromwell.port }}']
  restartPolicy: Never
