This is a placeholder file, as secrets cannot be checked. To obtain credentials:

docker run -it --rm \
-v $HOME:/root \
broadinstitute/dsde-toolbox:dev vault read -format=json secret/dsde/firecloud/dev/cromwell/cromwell-account.json

Then replace the contents of this file with the data JSON value.
